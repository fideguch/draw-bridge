/**
 * phaser-box2d@1.1.0 ships no TypeScript declarations (and a broken "main"
 * field — see the alias in vite.config.ts). Shorthand ambient declaration
 * until proper typings are generated in Phase 2 (engine physics work).
 */
declare module 'phaser-box2d';
