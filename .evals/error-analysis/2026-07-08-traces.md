# Error Analysis: 2026-07-08

> `templates/error-analysis.md` から生成。Phase 1 の必須ゲート（`<HARD-GATE id="EA">`）。
> **メトリクスより先に実トレースを読む。** 合成データは不可。
> SUT は決定論的なプロジェクトコード（Track A）が主。エージェント運用失敗は Track B として併記。

## 入力 (Inputs)

- trace source（実データのみ）:
  - `.fable/progress.md` — append-only 実装台帳（Phase 1〜11 の検証結果・発見バグ）
  - `.fable/decisions.md` — 可逆判断ログ（C1/M2/M3/L5 等の contract 整合記録）
  - `git log`（9ed819f..02635d4、22 commits on `001-inkbridge-mvp`）
  - `specs/001-inkbridge-mvp/research.md` §R10（S1-S3 スパイク結果・tuning 校正）
  - `.fable/criteria.json`（AC-1..AC-9 の合否 + evidence）
- 件数: 15 失敗インスタンス（実履歴から抽出）。~100 には未達だが**全件が実トレース由来**（合成ゼロ）。
  15 未満のカテゴリは capability 由来ではなく既修正の regression なので `UNVALIDATED` 扱い不要。
  Track A（コード）12 件 + Track B（エージェント運用）3 件。

## Step 1: Open Coding（自由記述、分類しない）

| #  | trace ref | 観察した失敗（自由記述） |
|----|-----------|--------------------------|
| 1  | progress.md 2A / decisions C1 (2026-07-08) | phaser-box2d が 32 world/process のスロット上限を超えると `b2CreateWorld` が黙って `b2WorldId(0,0)` を返し、33 回目の生成で vendor 内部の不透明な TypeError で落ちる（実機再現）。ガード + `reset()` ライフサイクルで修正。 |
| 2  | decisions 2026-07-07 / progress Phase 1 | phaser-box2d の package `main` フィールドが壊れており import 不能。Vite alias → `dist/PhaserBox2D.js` + 手書き `.d.ts` で回避。 |
| 3  | progress.md 2B (LIB-QUIRK) | wheel motor が sleeping body を起こさない。`engageMotor` で明示的に `b2Body_SetAwake` が必要（Box2D v3 期待挙動と乖離）。 |
| 4  | progress.md 2A | open-chain の一方向 collision winding。Terrain 側で winding を反転しないと片面しか衝突しない。 |
| 5  | progress Phase 4 / criteria AC-3 / contracts gate-pipeline.md | Gate 3 の anti-dominance bot が rim-to-rim ストロークを使い overlap ゼロ → 常に落下 → dominance gate が**空虚に pass**。overlap 拡張 {0,1,2}m の校正 + 負の対照（negative-control）regression test で修正。 |
| 6  | progress 2C / research R10 / decisions 2026-07-07 | provisional `breakForceFactor` 2.5 だと 4m ギャップが物理的に横断不能（1.79x force share で破断）。fixture を 4m→2m に縮小して糊塗 → spike sweep で factor 10 に校正して恒久修正。 |
| 7  | research R10 表 | chain method で N≥24 のとき chain が gap に滑り落ちる（構造定数の副作用）。segmentLength 0.8 に校正。 |
| 8  | progress Phase 3 (spike agent died pre-commit ~23:34) | 自律 spike サブエージェントが commit 直前に credit 枯渇で**無通知で死亡**。検出は file mtime + `ps` 突合により事後。remainder を main loop が回収・検証。 |
| 9  | task ledger item 5 | 並行エージェントの未完了編集による tsc ノイズが post-edit フック（型チェック）をブロック。 |
| 10 | progress / decisions M3 / 4a3de3b | Playwright の device descriptor（iPhone 14）が未インストールの webkit に既定フォールバック。chromium mobile emulation に切替（実 Safari は gatekeeper 手動ステップ）。 |
| 11 | decisions 2026-07-07 (Capacitor) | Capacitor 8 CLI が Node ≥22 を要求し Node 20 決定論 pin（constitution）と衝突 → Capacitor 7.6.7 に据え置き。 |
| 12 | Guardian review MEDIUM / progress | stress break 境界が実装 `>=1.0`、contract は `>1.0`。data-model 文言を実装に整合。 |
| 13 | task ledger item 6 / plan.md vs contracts gate-pipeline.md:74 | KPI-004/SC-004 の分母ドリフト: 「15 levels」と「18/18 levels」が混在（15 通常 + 3 bonus = 18）。 |
| 14 | Codex cross-review / decisions M3 | `launchReleased` イベントが motor engage より 1 tick 遅延（off-by-one）。anticipation→running 遷移前・first motor step の tick に固定して修正。 |
| 15 | Codex M2 / decisions M2 | segment count clamp が実装 `[1,32]`、contract は `[8,32]` 相当の想定と乖離。`[2,32]` に修正し ≥1 revolute joint を保証（+ terrain 重複頂点 validation gap）。 |

## Step 2: Axial Coding（6 個の具体的カテゴリに集約）

> 「temporal issues」ではなく「date-format error」の粒度で。カテゴリ ID は全 eval ファイルから機械参照される。

| taxonomy-id | カテゴリ名（canonical, 機械参照） | 説明 | open-coding # |
|-------------|----------------------------------|------|----------------|
| T1 | vendor-port API divergence | phaser-box2d（Box2D v3 JS port）が文書化/期待された API と乖離。silent failure・要明示 wake・winding・壊れた main field・スロット上限の黙殺。 | 1, 2, 3, 4 |
| T2 | contract-implementation drift | 凍結済み contract の文言・数値と実装がずれる。境界 `>=` vs `>`、分母 15 vs 18、event tick off-by-one、clamp 範囲。 | 12, 13, 14, 15 |
| T3 | physics-tuning gameplay regression | 計測なしで選んだ tuning 定数がレベルをプレイ不能/誤判定にする。証拠なしのハンドチューニング。 | 6, 7 |
| T4 | vacuous verification | 検証が対象プロパティを実際には行使せず pass する（空虚な合格）。負の対照の欠如。 | 5 |
| T5 | agent-ops mortality (Track B) | 無人自律運用中のエージェントプロセス障害（credit 枯渇での無通知死・並行編集ノイズ）。 | 8, 9 |
| T6 | cross-env toolchain assumptions | ツールが環境依存リソース（未インストール webkit・Node バージョン）に暗黙依存/フォールバックする。 | 10, 11 |

## Step 3: 頻度集計（Count & Prioritize）

| taxonomy-id | カテゴリ | 件数 | 全体比 | 優先度 | 重大度メモ |
|-------------|---------|------|--------|--------|------------|
| T2 | contract-implementation drift | 5 | 33% | P1 | 高頻度・静かに蓄積。契約テストで恒久ガード可能。 |
| T1 | vendor-port API divergence | 4 | 27% | P1 | 高頻度。全件 regression test で固定済み。 |
| T5 | agent-ops mortality | 3* | 20% | P2 | Track B。検出・耐障害はプロセス設計課題（skill-feedback へ）。 |
| T6 | cross-env toolchain assumptions | 2 | 13% | P2 | CI/device 環境の pin と明示で予防。 |
| T3 | physics-tuning gameplay regression | 2 | 13% | P1 | **低頻度・最高重大度**（レベルが解けなくなる）。determinism + spike で固定。 |
| T4 | vacuous verification | 1 | 7% | P1 | **低頻度・最高重大度**（検証自体が無力化）。negative-control で固定。 |

> \*T5 は 2 件の open-coding + 「事後検出でしか気づけなかった」というメタ失敗を 1 件計上（=3）。
> べき乗則どおり少数カテゴリ（T2+T1 = 60%）が失敗の大半。ただし **T3/T4 は頻度が低くても重大度で最上位**
> （1 件でも「ゲームが解けない」「検証が嘘をつく」に直結）→ 頻度ではなく **impact 順**で eval 化する。

## Step 4: saturation 判定

- [x] 直近の実トレース（Phase 4〜11、git 02635d4 まで）で新カテゴリが増えていない（= 6 カテゴリで飽和）。
- 追加トレースが到着したら（scheduled run が taxonomy 外の失敗を出したら）Phase 1 を再入する（Phase 7 / criteria drift）。

## 出力 (Outputs) — eval-spec を起こす対象（impact 順）

1. **T3 → determinism-1000 + vitest-suite**: 物理 tuning の再現性を bit-determinism（1000-run, hash `1e090a5c`）と engine unit で固定。
2. **T4 → gates-exit-0（gate3 negative-control 内包）**: anti-dominance bot が空虚に pass しないことを負の対照で固定。
3. **T2 → vitest-suite（contract テスト）+ gates-exit-0**: 契約値・境界・event tick を契約テスト/Gate で固定。
4. **T1 → vitest-suite + determinism-1000**: port quirk 群の regression test を全件緑で維持。
5. **T6 → tempo-contracts (E2E)**: device/toolchain 前提を E2E で固定（composition 進行中のため `PENDING`/`UNVALIDATED`）。
6. **T5（Track B）→ eval 化せず** `feedback/skill-feedback.md` に運用改善 PROPOSAL として送る（コード SUT では測れない、エージェント運用の失敗モード）。
